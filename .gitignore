# macOS
.DS_Store

# per-machine Claude Code permissions, not project config
.claude/settings.local.json

# pre-redesign snapshot of the old site
.backup-pre-figma/

# secrets — API keys live in Vercel env vars, never in the repo
.env
.env.*
.vercel
